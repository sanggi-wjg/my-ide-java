name: ✅ Build Test

on:
  push:
    branches: [ main, feature/** ]

jobs:
  build:
    runs-on: ubuntu-latest
#    permissions:
#      contents: read
#      packages: write

    steps:
#      - name: Build Essential
#        run: |
#          sudo apt-get update && sudo apt-get upgrade -y
#          sudo apt-get install -y moby-containerd moby-runc
#          sudo apt-get update && sudo apt-get install -y docker.io docker-compose

      - name: Checkout code
        uses: actions/checkout@v3

      - name: 🚀 Docker-Compose UP
        run: |
          docker-compose -f src/main/resources/container/docker-compose.yml up -d
          docker-compose -f src/main/resources/container/docker-compose-mysql.yml up -d

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'corretto'

      - name: Grant Gradle
        run: chmod +x ./gradlew

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
        with:
          arguments: build
          cache-read-only: true

      - name: Test Executing
        run: ./gradlew --info -Dspring.profiles.active=dev test

      - name: Upload Jacoco Report
        if: ${{ failure() }}
        uses: actions/upload-artifact@v3
        with:
          name: jacoco-report
          path: build/reports/jacoco/test/html

#      - name: Login to Docker Hub
#        uses: docker/login-action@v1
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}
#
#      - name: Build and push Docker image
#        id: docker_build
#        uses: docker/build-push-action@v2
#        with:
#          context: .
#          push: true
#          tags: ${{ secrets.DOCKER_USERNAME }}/my-image:${{ github.sha }}

